import Link from 'next/link';
export default function PageShell({active,title,description,children}:{active:string;title:string;description:string;children:React.ReactNode}){return <><header className="header"><Link className="wordmark" href="/">Lukas Tadros<span className="dot">.</span></Link><nav aria-label="Main navigation">{[['Home','/'],['School','/school'],['Projects','/projects'],['Travel','/travel']].map(([label,url])=><Link key={url} href={url} aria-current={active===label?'page':undefined}>{label}</Link>)}</nav></header><main id="main"><section className="page-title"><h1>{title}</h1><p>{description}</p></section>{children}</main><footer><Link href="/">Lukas Tadros</Link><span>A work in progress.</span></footer></>}
