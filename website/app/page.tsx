import Link from 'next/link';
import {readContent} from '../lib/content';
export const dynamic='force-dynamic';
export default async function Home(){const {content}=await readContent();const c=content.copy;return <><header className="header"><Link className="wordmark" href="/">{c.name}<span className="dot">.</span></Link><nav aria-label="Main navigation"><Link aria-current="page" href="/">Home</Link><Link href="/school">School</Link><Link href="/projects">Projects</Link><Link href="/travel">Travel</Link></nav></header><main id="main"><section className="intro"><div><h1>{c.homeTitle}<br/><em>{c.homeName}</em></h1><p className="lede preserve-lines">{c.intro}</p><p className="muted preserve-lines">{c.summary}</p><Link className="text-link" href="/projects">{c.projectLink} <span aria-hidden="true">↗</span></Link></div><figure className="portrait">{content.portrait?<img className="portrait-image" src={content.portrait.src} alt={content.portrait.alt}/>:<div className="portrait-placeholder"><span>LT</span><p>Portrait coming soon</p></div>}<figcaption>{c.portraitCaption}</figcaption></figure></section><section className="about"><h2>{c.aboutTitle}</h2><div><p className="large-copy preserve-lines">{c.bioLead}</p><p className="muted preserve-lines">{c.bio}</p></div></section><section className="directory" aria-label="Explore the site">{[[c.schoolLink,c.schoolLinkDescription,'/school'],[c.projectsLink,c.projectsLinkDescription,'/projects'],[c.travelLink,c.travelLinkDescription,'/travel']].map(([title,copy,url])=><Link href={url} key={url}><h2>{title}</h2><p>{copy}</p><span aria-hidden="true">↗</span></Link>)}</section></main><footer><span>{c.name}</span><span>{c.footer}</span></footer></>}
